{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Maven: package plugin (NetworkStorage)",
      "type": "shell",
      "command": "mvn -f NetworkStorage/pom.xml clean package",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "problemMatcher": [],
      "group": {
        "kind": "build",
        "isDefault": true
      }
    }
  ]
}
